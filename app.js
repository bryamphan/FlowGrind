// importing react hooks
const { useState, useEffect } = React;

// Main App Component
function App() {
    // state variables here todo
    
    return (
        <div>
            <h1>FlowGrind</h1>
            {/* build app here */}
        </div>
    );
}

// app render
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
